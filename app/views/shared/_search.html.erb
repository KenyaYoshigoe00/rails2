<h2>検索結果</h2>

<table>
  <thead>
    <tr>
      <th>画像　</th>
      <th>施設名　</th>
      <th>住所　</th>
      <th>施設詳細　</th>
      <th>宿泊料金　</th>
    </tr>
  </thead>

  <tbody>
    <% @rooms.each do |room| %>
      <tr>
        <td>
        <% if room.room_icon.attached? %>
          <%= image_tag room.room_icon, class:"room_image_size3" %>
        <% else %>
          <%= image_tag 'default_room_image.png', class:"room_image_size3" %>
        <% end %>
        </td>
        <td><%= room.name %></td>
        <td><%= room.address %></td>
        <td><%= room.introduction %></td>
        <td>¥<%= room.price %>~</td>
        <td><%= link_to "施設詳細・予約", room %></td>
      </tr>
    <% end %>
  </tbody>
</table>